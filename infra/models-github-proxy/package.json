{
  "name": "models-github-proxy",
  "private": true,
  "version": "0.1.0",
  "description": "Cloudflare Worker CORS proxy for the GitHub Models API used by the Narrarium web app.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
