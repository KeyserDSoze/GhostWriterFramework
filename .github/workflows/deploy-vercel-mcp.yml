name: Deploy Vercel MCP

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master
    paths:
      - api/**
      - vercel.json
      - package.json
      - package-lock.json
      - packages/core/**
      - packages/mcp-server/**

concurrency:
  group: deploy-vercel-mcp
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      VERCEL_ORG_ID: team_Jut3umUxSp1D1Z1sqmv4iUjc
      VERCEL_PROJECT_ID: prj_b9bxn0P4ilMmG40DiZoZ057qA1Zj

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Validate Vercel MCP build inputs
        run: npm run build -w @ghostwriter/core && npm run test -w @ghostwriter/mcp-server

      - name: Install Vercel CLI
        run: npm install --global vercel@latest

      - name: Pull Vercel environment
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: vercel pull --yes --environment=production --token="$VERCEL_TOKEN"

      - name: Deploy to Vercel
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: vercel deploy --prod --token="$VERCEL_TOKEN"
